import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    cardContainer:{
        marginBottom: 12.5,
        marginTop: 12.5,
        backgroundColor: '#F1F1F1',
        alignItems: 'center', 
    },
}) 